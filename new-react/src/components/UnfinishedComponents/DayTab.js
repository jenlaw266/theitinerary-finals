import Tab from "@mui/material/Tab";
import Typography from "@mui/material/Typography";

const DayTab = (props) => {
  return <Tab label={props.label} id={props.id}></Tab>;
};

export default DayTab;
